%h1 Login

= form_tag login_create_path, :method => 'post' do

  = label :session, :user_id, 'User-ID'
  = text_field :session, 'user_id', :id => 'login_user'

  = label :session, :email, 'E-Mail'
  = email_field :session, 'email', :id => 'login_email'

  = submit_tag 'Login to my account', :id => 'login_submit'
  
  = link_to 'Sign-up for an account', new_user_path, :id => 'signup'